# 🎵 Echo · 回声 - 独立音乐创作社区 (V4.0 AI 赋能版)

> **"念念不忘，必有回响。"**
>
> 一个基于 JavaWeb 标准技术栈 (MVC + 三层架构) 开发的独立音乐分享与社交平台。
> **V4.0 版本** 引入了 **DeepSeek AI** 和 **WebSocket 实时弹幕**，实现了从“传统 Web”到“智能化社交 Web”的跨越。

---

## ✨ 版本迭代亮点 (Highlights)

### 🤖 V4.0：AI 与 社交 (New!)
* **DeepSeek 音乐百科助手**：接入 DeepSeek 大模型 API，全站集成智能机器人。用户可随时询问音乐知识、歌手背景或寻求推荐，AI 实时响应。
* **一键社交分享**：播放页新增分享功能，一键生成当前音乐的专属链接，方便在社交网络传播。

### 🎨 V3.0：交互与视觉
* **APlayer 高颜值播放器**：集成封面显示、歌词滚动、波形进度条，告别原生丑陋控件。
* **B站同款弹幕**：基于 **WebSocket** 实现的时间轴弹幕。支持弹幕持久化存储、历史回溯（空降）、实时广播（全双工通信）。
* **数据可视化驾驶舱**：后台引入 **ECharts**，管理员可通过动态柱状图实时监控全站热歌数据。

### 🛡️ V2.0：业务逻辑核心
* **RBAC 权限管理**：游客/普通用户/超级管理员三级权限体系。
* **创作手记系统**：音乐人可以为自己的作品编写幕后故事（博客系统），加强社区深度。
* **审核机制**：用户上传的音乐默认“待审核”，需管理员批准后上线，确保内容安全。

---

## 🛠️ 技术栈 (Tech Stack)

* **后端**：Java (JDK 1.8), Servlet 3.1, JSP, JDBC, **WebSocket**, **OkHttp** (AI请求)
* **前端**：HTML5, CSS3, JavaScript, **AJAX**, **ECharts**, **APlayer**
* **数据库**：MySQL 5.7 / 8.0
* **构建工具**：Maven
* **服务器**：Tomcat 9.0

---

## 🚀 快速部署指南 (Deployment)

### 1. 环境准备
确保本地或服务器已安装：
* JDK 1.8+
* Tomcat 9.0
* MySQL 数据库

### 2. 数据库初始化
请在 MySQL 中执行项目根目录下的 **`musicdb.sql`** 脚本。
> 脚本将自动创建 `musicdb` 数据库、5张数据表，并内置默认管理员账号。

### 3. 核心配置 (必读!)

**A. 数据库连接**
打开 `src/main/java/com/music/util/DBUtil.java`：
```java
// 本地运行用 localhost，飞牛/Docker 部署请填局域网 IP
private static final String URL = "jdbc:mysql://localhost:3306/musicdb?...";
private static final String USER = "root";
private static final String PASS = "你的数据库密码"; //⚠️ 必改！
```
**B. AI 机器人配置 (DeepSeek)**
打开 `src/main/java/com/music/controller/AIServlet.java`：

```java
// ⚠️ 必须填写有效的 API Key，否则机器人无法回复
private static final String API_KEY = "sk-你的DeepSeek-API-Key";
```
### 4. 启动运行
1.  使用 IDEA 打开项目，等待 Maven 下载依赖（含 OkHttp, Gson, WebSocket）。
2.  配置 Tomcat Server，部署 Artifact `MusicWeb:war exploded`。
3.  **注意**：Application Context (访问路径) 请务必设置为 **`/MusicWeb`**。
4.  启动服务器，访问 `http://localhost:8080/MusicWeb`。

---

## 🧪 测试流程 & 账号

| 角色 | 账号 | 密码 | 权限范围 |
| :--- | :--- | :--- | :--- |
| **超级管理员** | `admin` | `123456` | 审核音乐、删除内容、查看数据大屏、管理手记 |
| **普通用户** | (请注册) | (任意) | 上传、评论、弹幕、写手记、调戏AI机器人 |

**功能体验建议：**
1.  **AI 问答**：点击页面右下角的 🤖 图标，问它：“推荐几首周杰伦的歌”。
2.  **弹幕互动**：用两个浏览器登录不同账号进入同一首歌，A 发送弹幕，B 无需刷新即可看到。
3.  **数据大屏**：登录 admin 账号，点击首页顶部的 `[管理]` -> `查看数据可视化大屏`。

---

## 📂 核心目录结构
```text
src/main/java/com/music/
├── bean/          # 实体类 (Model)
├── dao/           # 数据访问层 (DAO)
├── service/       # 业务逻辑层 (Service)
├── controller/    # 控制器 (Servlet)
│   ├── AIServlet.java        # DeepSeek AI 代理
│   ├── DanmakuServer.java    # WebSocket 弹幕服务
│   └── ...                   
└── util/          # 工具类 (DBUtil)
```
## 📝 关于项目
本项目为 JavaWeb 课程设计作品，旨在探索传统 Web 技术与现代 AI、实时交互技术的融合。

