# 🎵 Echo · 回声 - 独立音乐创作社区 (V3.0)

> **"念念不忘，必有回响。"**
>
> 一个基于 JavaWeb 标准技术栈 (MVC + 三层架构) 开发的独立音乐分享与社交平台。不仅是播放器，更是创作者与听众连接的桥梁。

---

## ✨ V3.0 核心亮点 (Key Features)

### 🎨 极致视觉体验
* **APlayer 高颜值播放器**：集成封面显示、歌词滚动、波形进度条，告别原生丑陋控件。
* **现代化 UI 设计**：全站采用渐变色、卡片式布局、悬停微交互，并在首页集成 **动态时钟**。
* **数据可视化驾驶舱**：后台引入 **ECharts**，管理员可通过动态柱状图实时监控全站热歌数据。

### 🚀 硬核技术实现
* **实时弹幕 (WebSocket)**：实现了 Bilibili 同款的时间轴弹幕。支持弹幕持久化存储、历史回溯、实时广播（全双工通信）。
* **异步交互 (AJAX)**：评论、点赞、数据加载均采用异步请求，无感刷新。
* **文件下载**：支持源文件下载，并自动重命名为友好格式 (歌手-歌名.mp3)。

### 🛡️ 完善的业务逻辑
* **RBAC 权限管理**：
    * **游客**：仅浏览、试听。
    * **用户**：上传音乐、发布评论、发送弹幕、撰写创作手记。
    * **管理员**：审核下架音乐、查看数据大屏。
* **创作手记系统**：音乐人可以为自己的作品编写幕后故事（博客系统），加强社区深度。
* **审核机制**：用户上传的音乐默认“待审核”，需管理员批准后上线，确保内容安全。

---

## 🛠️ 技术栈 (Tech Stack)

* **后端**：Java (JDK 1.8), Servlet 3.1, JSP, JDBC, **WebSocket**
* **前端**：HTML5, CSS3, JavaScript, **AJAX**, **ECharts**, **APlayer**
* **数据库**：MySQL 5.7 / 8.0
* **构建工具**：Maven
* **服务器**：Tomcat 9.0

---

## 🚀 快速部署 (Quick Start)

### 1. 环境准备
确保本地或服务器已安装：
* JDK 1.8+
* Tomcat 9.0
* MySQL 数据库

### 2. 数据库初始化
请在 MySQL 中执行项目根目录下的 **`musicdb.sql`** 脚本。
> 脚本将自动创建 `musicdb` 数据库、5张数据表，并内置默认管理员账号。

### 3. 项目配置
打开 `src/main/java/com/music/util/DBUtil.java`，修改数据库连接信息：
```java
// 如果是本地运行，保持 localhost；如果是 Docker/云服务器，请填写实际 IP
private static final String URL = "jdbc:mysql://localhost:3306/musicdb?...";
private static final String USER = "root";
private static final String PASS = "你的数据库密码"; // ⚠️ 必改！